** STORE THE DATASET and the CODE FILES in the SAME FOLDER **

1. Update the "DATASET Path" and the "FILE PATH" of the CODE in FACE MASK.py File.

2. Better to execute the file in SPYDER.

3. Since we're having large dataset, it'll take around 10 - 12 hrs to train.

3. Then, it'll create two files with the exetensions ".json" and ".h5" in the project folder

4. Now, nxecute the FACE MASK (json file code).py code file

5. Then a Camera window is popped up show u the result.


